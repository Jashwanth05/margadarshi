{
  "build": {
    "env": {
      "PYTHON_VERSION": "3.10"
    },
    "commands": [
      "bash .vercel_build.sh"
    ]
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/app" }
  ]
}
